# project-c/charts/demo-app/templates/dashboard-provisioning-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-grafana-dash-config
  namespace: project-c
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: 'default'
        orgId: 1
        folder: 'Project C Dashboards'
        type: file
        options:
          path: /opt/bitnami/grafana/provisioning/dashboards/custom

